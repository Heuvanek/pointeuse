<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %}SRDEV POINTEUSE{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootswatch/4.4.1/darkly/bootstrap.css" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.12.1/css/all.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}" />
    <link rel="stylesheet" href="{{ asset('css/normalize.css') }}" />

    {% block stylesheets %} {% endblock %}
</head>

<body>
{% if is_granted('ROLE_ADMIN') %}
    {% block header %}
    <nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4">
        <a class="navbar-brand" href="{{ url('pointeuse') }}"><img src="{{ asset('img/logosr.png')}}" alt="Le logo de SRDEV"
                class="logo-base"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse"
            aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <form class="form-inline mt-2 mt-md-0">
                <input class="form-control mr-sm-2" type="text" placeholder="Rechercher" aria-label="Rechercher">
            </form>
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('pointeuse') }}">Pointeuse</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('new_client') }}">Nouveau client</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('listing_client',{id_user: app.user.id}) }}">Listing clients</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('historic',{id_user: app.user.id}) }}">Historique mensuel</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('app_logout') }}">Déconnexion</a>
                </li>
            </ul>
        </div>
    </nav>
    {% endblock %}
    <div class="container">
        {% block body %}
        {% endblock %}
    </div>
    {% else %}
    <div class="d-flex justify-content-center">
        <div class="col-6 col-sm-4 mt-5">
            <div class="alert alert-danger">
            <i class="far fa-grin-beam-sweat"></i>
                <strong>Bien tenté ! </strong>
                Vous n'êtes pas autorisé à acceder à cette page, veuillez vous connecter.
                <a href="{{ url('app_login') }}">Cliquez ici</a>
            </div>
        </div>
    </div>
    {% endif %}
    {% block footer %}
    <div class="footer fixed-bottom row">
        <div class="footer-copyright text-center py-3 col-4">
            <a href="https://srdev.fr/" class="text-info">Contact</a>
        </div>
        <div class="footer-copyright text-center py-3 col-4">© 2020 Copyright:
            <a href="https://srdev.fr/" class="text-info"> SRDev</a>
        </div>
        <div class="footer-copyright text-center py-3 col-4">
            <a href="https://srdev.fr/" class="text-info">Mentions légales</a>
        </div>
    </div> {% endblock %}
    <script src="https://code.jquery.com/jquery-3.3.1.js"></script>
    {% block javascripts %}{% endblock %}
</body>

</html>